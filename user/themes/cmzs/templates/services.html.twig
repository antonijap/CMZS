{% extends 'base.html.twig' %} {% block content %}

<div class="container services">
    <div class="row">
        <div class="col">
            <h1>{{ page.title }}</h1>
            {{ page.content }}
        </div>
    </div>
</div>
</div>
{% endblock %}
